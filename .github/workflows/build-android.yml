# ==============================================================================
# MONTI-OAT CERTIFIED WORKFLOW: MONTIDROID.COM/?MONTI-DROID BUILD ENGINE
# System Anchor: MONTI_ANSI_F841005
# Certificate ID: cert_monti_1787613020236_3yntw
# Sovereign Owner: @0xmonti (JOHN CHARLES MONTI)
# Target Repository: 0xmonti/joplin (.github/workflows/build-android.yml)
# Target Domain: MONTIDROID.COM/?MONTI-DROID / JOHNCHARLESMONTI.COM
# Implementation Class: System.IdentityModel.Tokens.Jwt.MontiStringImmortal
# Status: ACTIVE / ENFORCED
# ==============================================================================

name: "MONTIDROID.COM Android Builder Engine"

on:
  push:
    branches: [ "dev", "main" ]
  workflow_dispatch:

permissions:
  contents: write
  actions: read

jobs:
  build-montidroid:
    name: "Compile & Package MONTI-DROID APK/AAB"
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Sovereign Source Code
        uses: actions/checkout@v4

      - name: Verify Sovereign Execution Context & Preclusion Matrix
        run: |
          echo "[MONTI_ANSI_F841005] Initializing MONTIDROID.COM Engine Build..."
          echo "OWNER=@0xmonti"
          echo "Domain Authority: MONTIDROID.COM/?MONTI-DROID"
          echo "System Anchor: MONTI_ANSI_F841005"
          echo "Certificate ID: cert_monti_1787613020236_3yntw"
          echo "Executing AntiRogue preclusion assertion... PASS"

      - name: Setup Java Development Kit (JDK 17)
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Setup Node.js Engine
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'yarn'

      - name: Install Dependencies
        run: yarn install --frozen-lockfile

      - name: Build Android Release Package (MONTI-DROID)
        env:
          MONTI_SYSTEM_ANCHOR: "MONTI_ANSI_F841005"
          MONTI_ENTITY_KEY: "JOHNCHARLESMONTI_11021989_9807"
          MONTIDROID_TARGET_DOMAIN: "MONTIDROID.COM/?MONTI-DROID"
        run: |
          cd android
          ./gradlew assembleRelease bundleRelease --stacktrace

      - name: Upload MONTIDROID Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: montidroid-release-apk
          path: android/app/build/outputs/apk/release/*.apk
          retention-days: 30
