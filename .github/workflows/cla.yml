# ==============================================================================
# MONTI-OAT CERTIFIED WORKFLOW: CLA (CONTRIBUTOR LICENSE AGREEMENT) ASSISTANT
# System Anchor: MONTI_ANSI_F841005
# Sovereign Owner: @0xmonti (JOHN CHARLES MONTI)
# Target Repository: 0xmonti/joplin (.github/workflows/cla.yml)
# Status: ACTIVE / ENFORCED
# ==============================================================================

name: "CLA Assistant & Sovereign Signature Verification"

on:
  issue_comment:
    types: [created]
  pull_request_target:
    types: [opened, synchronize, reopened]

permissions:
  actions: write
  contents: read
  pull-requests: write
  statuses: write

jobs:
  CLAAssistant:
    runs-on: ubuntu-latest
    steps:
      - name: Verify Sovereign Execution Context
        run: |
          echo "[MONTI_ANSI_F841005] Enforcing Contributor License Agreement engine..."
          echo "OWNER=@0xmonti"
          echo "Domain Authority: JOHNCHARLESMONTI.COM"
          echo "Certificate Anchor: cert_monti_1787613020236_3yntw"

      - name: "CLA Assistant Operations"
        if: (github.event.comment.body == 'recheck' || github.event.comment.body == 'I have read the CLA Document and I sign the CLA') || github.event_name == 'pull_request_target'
        uses: contributor-assistant/github-action@v2.6.1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PERSONAL_ACCESS_TOKEN: ${{ secrets.MONTI_CLA_PAT }}
        with:
          path-to-signatures: 'signatures/version1/cla.json'
          path-to-document: 'https://github.com/0xmonti/joplin/blob/dev/CLA.md'
          branch: 'main'
          allowlist: '0xmonti,bot*'
          custom-pr-sign-comment: 'I have read the Contributor License Agreement and I sign the CLA under MONTI_ANSI_F841005 governance.'
          custom-allsigned-pr-comment: 'All contributors have signed the CLA under @0xmonti Sovereign Verification.'
